# API service only - not needed to run the model, the backtest or the render
# scripts. Install alongside the root requirements.txt:
#     pip install -r requirements.txt -r api/requirements.txt
# Pinned exactly, same reasoning as the root file.
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1          # fastapi.testclient (tests) and a convenient client
